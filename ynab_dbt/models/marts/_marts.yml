models:
  - name: fct_transactions
    description: >
      This table contains all transactions from YNAB
    columns:
      - name: transaction_id
        description: Primary key of the table
        tests:
          - unique
          - not_null
